import "./Header.css"

const Header = () =>{
    return(
        <header>
            <div className="weather-app">
                <h1>Weather App</h1>
                <button>Login</button>
            </div>
        </header>
    );
}
export default Header;